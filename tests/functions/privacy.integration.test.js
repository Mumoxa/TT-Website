import { expect,it,vi } from 'vitest';
import { onRequest } from '../../functions/api/cv-suggestions.js';
it('never echoes candidate marker on upstream failure',async()=>{vi.stubGlobal('fetch',vi.fn(async(_url,init)=>{expect(init.headers.Authorization).toBe('Bearer relay-token');expect(init.headers['X-Client-Key']).toMatch(/^[a-f0-9]{64}$/);throw new Error('proxy PRIVATE-CANDIDATE-MARKER-7391')}));const request=new Request('https://site/api/cv-suggestions',{method:'POST',headers:{'Content-Type':'application/json','CF-Connecting-IP':'198.51.100.1'},body:JSON.stringify({task:'refine_bullet',fragments:[{id:'x',text:'PRIVATE-CANDIDATE-MARKER-7391'}]})});const response=await onRequest({request,env:{ORACLE_CV_API_URL:'http://api',ORACLE_CV_API_TOKEN:'relay-token',CLIENT_KEY_SECRET:'secret'}});expect(response.status).toBe(503);expect(await response.text()).not.toContain('PRIVATE-CANDIDATE-MARKER-7391');});
