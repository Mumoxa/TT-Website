const skills=[['Systems & integration',57],['Aeronautical / mechanical / aerostructures',45],['GCS / mission software',39],['Production / industrialisation / PLM',37],['Electronics hardware',29],['Test & verification',28],['Embedded C/C++ & firmware',19],['RF / EW / communications',16],['Avionics / AIT',13],['Controls / autonomy',8],['DSP / FPGA / VHDL',3],['Composites',2]];
const techStacks=[['Systems',58],['C / C++',54],['Integration',51],['Mechanical',39],['Electronics',38],['Python',27],['Linux',27],['Java',26],['Embedded / Firmware',25],['CAD',21],['Radar',20],['RTOS',15],['RF / Antenna',15],['Controls / ADCS',14],['Avionics',10],['Teamcenter / PLM',4],['Composites',3],['FPGA / VHDL',2]];
const skillBox=document.getElementById('skillsBars'); const maxSkill=Math.max(...skills.map(x=>x[1]));
skills.forEach(([name,val])=>{const el=document.createElement('div');el.className='skill';el.innerHTML=`<div class="skillName">${name}</div><div class="barTrack"><i class="barFill" data-w="${(val/maxSkill*100).toFixed(1)}"></i></div><div class="skillVal">${val}</div>`;skillBox.appendChild(el)});
const bubbleField=document.getElementById('bubbleField');
const placements=[[10,8],[28,8],[48,7],[68,8],[84,10],[6,40],[23,39],[39,38],[56,38],[72,40],[87,40],[14,70],[31,69],[47,69],[62,69],[76,70],[89,70],[37,85],[62,85]];
techStacks.forEach(([name,val],i)=>{const d=document.createElement('div');const size=Math.max(62,Math.min(126,48+val*1.25));d.className='bubble';d.style.width=size+'px';d.style.height=size+'px';d.style.left=placements[i][0]+'%';d.style.top=placements[i][1]+'%';d.style.transform='translate(-50%,-50%)';d.style.animationDelay=(i*.19)+'s';d.innerHTML=`<div><b>${name}</b><span>${val}</span></div>`;bubbleField.appendChild(d)});
const obs=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');e.target.querySelectorAll?.('.barFill').forEach(b=>b.style.width=b.dataset.w+'%');if(e.target.classList.contains('skillPanel')) e.target.querySelectorAll('.barFill').forEach((b,i)=>setTimeout(()=>b.style.width=b.dataset.w+'%',i*45));e.target.querySelectorAll?.('.counter').forEach(runCounter);if(e.target.classList.contains('counter'))runCounter(e.target)}})},{threshold:.13});document.querySelectorAll('.reveal,.counter').forEach(el=>obs.observe(el));
function runCounter(el){if(el.dataset.done)return;el.dataset.done=1;const end=+el.dataset.count;const t0=performance.now(),dur=1100;function step(t){const p=Math.min(1,(t-t0)/dur),e=1-Math.pow(1-p,3);el.textContent=Math.round(end*e).toLocaleString();if(p<1)requestAnimationFrame(step)}requestAnimationFrame(step)}
window.addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-innerHeight;document.getElementById('progress').style.width=(scrollY/h*100)+'%'});
const canvas=document.getElementById('network'),ctx=canvas.getContext('2d');let W,H,nodes=[];function resize(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);nodes=Array.from({length:Math.min(75,Math.floor(innerWidth/20))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18,r:Math.random()*1.5+.5}))}resize();addEventListener('resize',resize);function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const n of nodes){n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>innerWidth)n.vx*=-1;if(n.y<0||n.y>innerHeight)n.vy*=-1;ctx.beginPath();ctx.arc(n.x,n.y,n.r,0,Math.PI*2);ctx.fillStyle='rgba(143,208,226,.24)';ctx.fill()}for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<125){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(19,101,121,${.055*(1-d/125)})`;ctx.lineWidth=.7;ctx.stroke()}}requestAnimationFrame(draw)}if(!matchMedia('(prefers-reduced-motion: reduce)').matches)draw();
// Explicit mail action for the client CTA. Keep the native mailto href for accessibility and add a click fallback.
document.addEventListener('click',e=>{const a=e.target.closest?.('[data-email-link]');if(!a)return;e.preventDefault();window.location.href=a.getAttribute('href');});
