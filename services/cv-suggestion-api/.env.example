# Generate a long random relay secret; use the same value in the Cloudflare secret.
CV_RELAY_TOKEN=
# Set only after confirming the installed identifier with `ollama list`.
OLLAMA_MODEL=
OLLAMA_BASE_URL=http://host.containers.internal:11434
# Existing private reverse-proxy network, confirmed with the evidence commands below.
PROXY_NETWORK=
