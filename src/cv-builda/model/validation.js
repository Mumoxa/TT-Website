export function validateForExport(s){const errors=[];if(!s.file)errors.push('Upload a CV first.');if(s.fragments.some(f=>f.coverage==='unreviewed'))errors.push('Review every source fragment.');if(s.notes.some(n=>n.status==='pending'))errors.push('Decide every additional note.');s.experience.forEach((x,i)=>{if(!x.employer||!x.title||!x.duration)errors.push(`Complete work experience ${i+1}.`)});return {valid:errors.length===0,errors};}
