const months={jan:1,feb:2,mar:3,apr:4,may:5,jun:6,jul:7,aug:8,sep:9,oct:10,nov:11,dec:12};
export function normaliseDateRange(input){const original=input.trim(),value=original.replace(/\bcurrent\b/ig,'Present');const parts=value.split(/\s*(?:–|—|-)\s*/);return {original,value,start:parse(parts[0]),end:/present/i.test(parts[1]||'')?Infinity:parse(parts[1]),comparable:parts.length===2&&parse(parts[0])!=null&&(/present/i.test(parts[1])||parse(parts[1])!=null)};}
function parse(s=''){const y=s.match(/\b(19|20)\d{2}\b/)?.[0];if(!y)return null;const m=Object.entries(months).find(([k])=>s.toLowerCase().includes(k))?.[1];return Number(y)*100+(m||0);}
export function sortWorkExperience(records){const decorated=records.map((r,i)=>({r,i,d:normaliseDateRange(r.duration||'')}));if(decorated.some(x=>!x.d.comparable))return {records,warnings:['Some dates could not be safely compared; source order was preserved.']};return {records:decorated.sort((a,b)=>b.d.end-a.d.end||b.d.start-a.d.start).map(x=>x.r),warnings:[]};}
